import React from "react";

import ringring from '../../assets/images/ringring.png'
import './Notification.css'

const Notification = () => {
    return (
        <img src = {ringring} alt = "Notification" className = "notification"></img>
    );
};

export default Notification;
